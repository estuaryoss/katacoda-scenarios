In this step we will apply the deployment plan.

Let's issue a 'apply --help'  
`./seeder apply --help`{{execute}}

Let's issue a 'apply'  
`./seeder apply`{{execute}}


After the seeder created the deployments you can print the current status:  
`./seeder show`{{execute}}
